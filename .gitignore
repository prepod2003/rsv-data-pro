# Этот каталог уезжает ОТДЕЛЬНЫМ ПУБЛИЧНЫМ репозиторием — сюда попадает только сайт.
# Правила ниже — страховка от случайно положенного лишнего файла: если внутренний
# материал окажется здесь по недосмотру, он хотя бы не уедет наружу.

# Рабочие каталоги разработки (их место — «Разработка сайта/» в основном репозитории)
_*/
материалы/
части-руководства/
заготовки/
снимки/

# Скрипты и служебное
*.py
*.ps1
*.cmd
*.bat
*.log

# Заметки и черновики
*.draft.*
*-черновик.*
TODO*
ЗАМЕТКИ*

# Секреты — здесь их быть не может по определению, но пусть правило стоит
.secrets/
*.token
*.key
*.pem

# Системное
.DS_Store
Thumbs.db
